{
  "name": "revise_server",
  "version": "1.0.0",
  "description": "The backend for my revision application",
  "license": "ISC",
  "author": "giiio",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf disr && cd ../revise_app && npm run build && cp -r dist ../revise_server",
    "deploy:full": "npm run build:ui && git add . && git commit -m uiBuild && git push "
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.4"
  }
}
